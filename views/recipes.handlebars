{{!-- need to link css / make the recipe names clearly clickable --}}
<h1>Recipes List</h1>

<div>
    {{#each recipes}}
      <div >
      <h1 class="recipe-name" data-id={{id}}>{{name}}</h1>
      <p>{{description}}</p>
      {{!-- step.details will show intructions --}} 
   <h1>End of Recipe</h1>
      </div>
    {{/each}}
  </div>
  <script src="/js/recipe.js"></script>